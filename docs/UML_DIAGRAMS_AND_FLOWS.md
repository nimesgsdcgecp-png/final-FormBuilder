# FormBuilder3: UML Diagrams and System Flows

## Table of Contents

1. [Entity Relationship Diagram (ERD)](#entity-relationship-diagram)
2. [Class Diagram](#class-diagram)
3. [Sequence Diagrams](#sequence-diagrams)
4. [Use Case Diagram](#use-case-diagram)
5. [State Diagrams](#state-diagrams)
6. [Component Diagram](#component-diagram)

---

## Entity Relationship Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        FORMBUILDER3 - ERD                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐           ┌──────────────────────┐
│    app_user      │           │      app_role        │
├──────────────────┤           ├──────────────────────┤
│ id (PK)          │           │ id (PK)              │
│ username         │───────┐   │ role_name            │
│ password_hash    │       │   │ description          │
│ email            │       │   └──────────────────────┘
│ active           │       │
│ created_at       │       │   ┌──────────────────────┐
│ updated_at       │       └──→│    user_role         │
└──────────────────┘           ├──────────────────────┤
                                │ user_id (FK)         │
┌──────────────────┐           │ role_id (FK)         │
│      form        │           └──────────────────────┘
├──────────────────┤
│ id (PK)          │
│ code             │
│ name             │           ┌──────────────────────┐
│ description      │───────┬──→│  form_version        │
│ status           │       │   ├──────────────────────┤
│ created_by       │       │   │ id (PK)              │
│ created_at       │       │   │ form_id (FK)         │
│ updated_at       │       │   │ version_number       │
└──────────────────┘       │   │ is_active            │
                            │   │ definition_json      │
                            │   │ created_by           │
                            │   │ created_at           │
                            │   └──────────────────────┘
                            │           │
                            │           ├─┐
                            │           │ └──────────────┐
                            │   ┌──────────────────┐    │
                            │   │  form_field      │    │
                            │   ├──────────────────┤    │
                            │   │ id (PK)          │    │
                            │   │ form_version(FK) │    │
                            │   │ field_key        │    │
                            │   │ label            │    │
                            │   │ field_type       │    │
                            │   │ is_required      │    │
                            │   │ is_read_only     │    │
                            │   │ default_value    │    │
                            │   │ display_order    │    │
                            │   │ config_json      │    │
                            │   └──────────────────┘    │
                            │                           │
                            │   ┌──────────────────┐    │
                            └──→│field_validation  │←───┘
                                ├──────────────────┤
                                │ id (PK)          │
                                │ form_version(FK) │
                                │ field_key        │
                                │ validation_type  │
                                │ expression       │
                                │ error_message    │
                                │ execution_order  │
                                │ scope            │
                                └──────────────────┘

┌──────────────────────────────┐
│  form_submission_meta        │
├──────────────────────────────┤
│ id (PK)                      │
│ form_id (FK)                 │
│ form_version_id (FK)         │
│ submission_table             │
│ submission_row_id            │
│ status                       │
│ submitted_by                 │
│ submitted_at                 │
│ created_at                   │
│ is_deleted                   │
└──────────────────────────────┘

┌──────────────────────────────┐
│  DYNAMIC TABLES              │
│  (form_data_{code})          │
├──────────────────────────────┤
│ id (PK)                      │
│ form_version_id              │
│ created_by                   │
│ created_at                   │
│ updated_at                   │
│ is_draft                     │
│ [field_key columns...]       │
└──────────────────────────────┘

┌──────────────────────────────┐
│  audit_log                   │
├──────────────────────────────┤
│ id (PK)                      │
│ user_id                      │
│ action                       │
│ entity_type                  │
│ entity_id                    │
│ changes (JSONB)              │
│ created_at                   │
└──────────────────────────────┘
```

---

## Class Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                    FORMBUILDER3 - CLASS DIAGRAM                    │
└────────────────────────────────────────────────────────────────────┘

DOMAIN MODELS
═════════════

┌─────────────────────┐
│      Form           │
├─────────────────────┤
│ - id: UUID          │
│ - code: String      │
│ - name: String      │
│ - description: String
│ - status: FormStatus│
│ - createdBy: String │
│ - createdAt: DateTime
│ - updatedAt: DateTime
│ - versions: List    │
├─────────────────────┤
│ + getActiveVersion()│
│ + publish()         │
│ + archive()         │
│ + addVersion()      │
└─────────────────────┘

┌──────────────────────┐
│   FormVersion        │
├──────────────────────┤
│ - id: UUID           │
│ - form: Form         │
│ - versionNumber: int │
│ - isActive: boolean  │
│ - definitionJson: JSON
│ - fields: List       │
│ - validations: List  │
│ - createdBy: String  │
│ - createdAt: DateTime│
├──────────────────────┤
│ + getActiveVersion() │
│ + addField()         │
│ + removeField()      │
│ + addValidation()    │
└──────────────────────┘

┌──────────────────────┐
│   FormField          │
├──────────────────────┤
│ - id: UUID           │
│ - formVersion: FK    │
│ - fieldKey: String   │
│ - label: String      │
│ - fieldType: String  │
│ - isRequired: boolean│
│ - isReadOnly: boolean│
│ - defaultValue: any  │
│ - displayOrder: int  │
│ - configJson: JSON   │
├──────────────────────┤
│ + validate()         │
│ + render()           │
│ + toSql()            │
└──────────────────────┘

┌─────────────────────────┐
│ FieldValidation         │
├─────────────────────────┤
│ - id: UUID              │
│ - formVersion: FK       │
│ - fieldKey: String      │
│ - validationType: String│
│ - expression: String    │
│ - errorMessage: String  │
│ - executionOrder: int   │
│ - scope: FIELD|FORM     │
├─────────────────────────┤
│ + evaluate()            │
│ + getErrorMessage()     │
└─────────────────────────┘

┌──────────────────────────┐
│ FormSubmissionMeta       │
├──────────────────────────┤
│ - id: UUID               │
│ - form: FK               │
│ - formVersion: FK        │
│ - submissionTable: String│
│ - submissionRowId: UUID  │
│ - status: DRAFT|SUBMITTED
│ - submittedBy: String    │
│ - submittedAt: DateTime  │
│ - createdAt: DateTime    │
│ - isDeleted: boolean     │
├──────────────────────────┤
│ + submit()               │
│ + saveDraft()            │
│ + getSubmissionData()    │
└──────────────────────────┘

SERVICES
════════

┌────────────────────┐
│  FormService       │
├────────────────────┤
│ + createForm()     │
│ + listForms()      │
│ + getForm()        │
│ + updateForm()     │
│ + deleteForm()     │
│ + publishForm()    │
│ + archiveForm()    │
└────────────────────┘

┌────────────────────┐
│  VersionService    │
├────────────────────┤
│ + createVersion()  │
│ + listVersions()   │
│ + activateVersion()│
│ + deactivate()     │
│ + cloneVersion()   │
└────────────────────┘

┌────────────────────┐
│ ValidationEngine   │
├────────────────────┤
│ + validate()       │
│ + evaluateField()  │
│ + evaluateForm()   │
│ + validateExpression()
└────────────────────┘

┌────────────────────┐
│ExpressionEvaluator│
├────────────────────┤
│ + evaluate()       │
│ + parse()          │
│ + validateAST()    │
│ + interpret()      │
└────────────────────┘

┌────────────────────┐
│ SchemaGenerator    │
├────────────────────┤
│ + createTable()    │
│ + alterTable()     │
│ + mapFieldToColumn()
│ + createIndexes()  │
│ + validateSchema() │
└────────────────────┘

┌────────────────────┐
│SubmissionService   │
├────────────────────┤
│ + saveDraft()      │
│ + submitForm()     │
│ + getSubmission()  │
│ + listSubmissions()│
│ + bulkDelete()     │
│ + exportCsv()      │
└────────────────────┘

┌────────────────────┐
│  AuditService      │
├────────────────────┤
│ + logAction()      │
│ + logChange()      │
│ + getAuditLog()    │
└────────────────────┘

CONTROLLERS
═══════════

┌─────────────────────┐
│  AuthController     │
├─────────────────────┤
│ POST /login         │
│ POST /logout        │
│ GET /me             │
└─────────────────────┘

┌─────────────────────┐
│  FormController     │
├─────────────────────┤
│ POST /forms         │
│ GET /forms          │
│ GET /forms/{id}     │
│ PUT /forms/{id}     │
│ DELETE /forms/{id}  │
│ POST /publish       │
└─────────────────────┘

┌─────────────────────┐
│ SubmissionController│
├─────────────────────┤
│ POST /draft         │
│ POST /submit        │
│ GET /submissions    │
│ POST /bulk          │
│ GET /export         │
└─────────────────────┘
```

---

## Sequence Diagrams

### 1. Form Creation & Publication Flow

```
User              Frontend          Backend            Database
 │                  │                  │                  │
 │─ Create Form ──→ │                  │                  │
 │                  │─ POST /forms ───→│                  │
 │                  │                  │─ Save Form ─────→│
 │                  │                 ←│─ Form created ───│
 │                  │←─ Form ID ────────│                  │
 │←─ Redirect ──────│                  │                  │
 │
 │─ Add Fields ───→ │                  │                  │
 │                  │─ PUT /fields ────→│                  │
 │                  │                  │─ Save Field ────→│
 │                  │←─ Success ────────│                  │
 │←─ Field Added ────│                  │                  │
 │
 │─ Publish ──────→ │                  │                  │
 │                  │─ POST /publish ──→│                  │
 │                  │                  │─ Validate ──────→│
 │                  │                  │←─ Valid ----------│
 │                  │                  │─ Create Table ───→
 │                  │                  │←─ Table created ──│
 │                  │                  │─ Update status ──→
 │                  │←─ Published ──────│                  │
 │←─ Success ────────│                  │                  │
```

### 2. Form Submission Flow

```
User              Frontend          Backend            Database
 │                  │                  │                  │
 │─ Fill Form ────→ │                  │                  │
 │ (client-side     │─ Validate ──────→│                  │
 │  validation)     │←─ Errors/OK ─────│                  │
 │                  │                  │                  │
 │─ Save Draft ───→ │                  │                  │
 │                  │─ POST /draft ────→│                  │
 │                  │                  │─ Check Draft ───→│
 │                  │                  │←─ Draft status ───│
 │                  │                  │─ INSERT/UPDATE ──→
 │                  │←─ Draft ID ───────│                  │
 │←─ Draft Saved ────│                  │                  │
 │
 │─ Submit ───────→ │                  │                  │
 │                  │─ POST /submit ───→│                  │
 │                  │                  │─ Validate ──────→│
 │                  │                  │← (server-side)───│
 │                  │                  │─ If valid: INSERT│
 │                  │                  │─ Record metadata ─│
 │                  │←─ Success ────────│                  │
 │←─ Thank You ──────│                  │                  │
```

### 3. Rule Evaluation Flow

```
Submission Data    Validation Engine    Expression Eval    Database
       │                   │                   │                │
       │─ Validate ───────→│                   │                │
       │                   │─ Get Validations →│────────────────→
       │                   │                   │←─ Rules ────────
       │                   │─ Parse Expr. ────→│
       │                   │←─ AST ────────────│
       │                   │─ Validate AST ───→│
       │                   │←─ Valid ──────────│
       │                   │─ Evaluate ───────→│
       │                   │  (with values)    │
       │                   │←─ Result ─────────│
       │                   │─ Collect Errors ─→│
       │←─ Result ─────────│                   │
```

---

## Use Case Diagram

```
┌──────────────────────────────────────────────────────┐
│           FORMBUILDER3 - USE CASES                   │
└──────────────────────────────────────────────────────┘

                         ┌─────────────┐
                         │    ADMIN    │
                         └─────────────┘
                               │
                    ┌──────────┼──────────┐
                    │          │          │
                    ▼          ▼          ▼
            ┌──────────┐ ┌──────────┐ ┌──────────┐
            │Create    │ │Publish   │ │Archive   │
            │Form      │ │Form      │ │Form      │
            └──────────┘ └──────────┘ └──────────┘
                    │          │          │
                    └──────────┼──────────┘
                               │
                         ┌─────────────┐
                         │  Manage     │
                         │  Forms      │
                         └─────────────┘

                         ┌─────────────┐
                         │  MENTOR     │
                         └─────────────┘
                               │
                    ┌──────────┼──────────┐
                    │          │          │
                    ▼          ▼          ▼
            ┌──────────┐ ┌──────────┐ ┌──────────┐
            │Edit      │ │Review    │ │Approve   │
            │Form      │ │Submission│ │Submission│
            └──────────┘ └──────────┘ └──────────┘

                         ┌─────────────┐
                         │   USER      │
                         └─────────────┘
                               │
                    ┌──────────┼──────────┐
                    │          │          │
                    ▼          ▼          ▼
            ┌──────────┐ ┌──────────┐ ┌──────────┐
            │Fill      │ │Save      │ │Submit    │
            │Form      │ │Draft     │ │Form      │
            └──────────┘ └──────────┘ └──────────┘
```

---

## State Diagrams

### Form State Machine

```
┌─────────────────────────────────────────────┐
│         FORM LIFECYCLE STATE MACHINE         │
└─────────────────────────────────────────────┘

              CREATE
               ↓
         ┌─────────────┐
    ┌───→│   DRAFT     │←───┐
    │    └─────────────┘    │
    │          │            │ EDIT
    │          │ PUBLISH    │
    │          ▼            │
    │    ┌─────────────┐    │
    │    │ PUBLISHED   │    │
    │    └─────────────┘    │
    │          │            │
    │          │ ARCHIVE    │
    └──────────┼────────────┘
               ▼
         ┌─────────────┐
         │  ARCHIVED   │
         └─────────────┘


    TRANSITION CONDITIONS:
    ├─ DRAFT → PUBLISHED: All fields valid, schema complete
    ├─ DRAFT → DRAFT: Edit form (no change of state)
    ├─ PUBLISHED → ARCHIVED: Archive command
    ├─ ARCHIVED → DRAFT: Unarchive (if allowed)
    └─ PUBLISHED ↔ DRAFT: Version changes create new draft
```

### Submission State Machine

```
┌──────────────────────────────────────────────┐
│      SUBMISSION LIFECYCLE STATE MACHINE       │
└──────────────────────────────────────────────┘

              CREATE
               ↓
         ┌─────────────┐
    ┌───→│   DRAFT     │←────┐
    │    └─────────────┘     │
    │          │             │ SAVE
    │          │ SUBMIT      │
    │          ▼             │
    │    ┌──────────────┐    │
    │    │  SUBMITTED   │    │
    │    └──────────────┘    │
    │          │
    │          │ DELETE
    └──────────┼─────────────→ DELETED


    TRANSITION CONDITIONS:
    ├─ DRAFT → DRAFT: Update data (no validation)
    ├─ DRAFT → SUBMITTED: Full validation passed
    ├─ SUBMITTED → DELETED: Soft delete
    └─ Once SUBMITTED, cannot revert to DRAFT
```

---

## Component Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│            FORMBUILDER3 - COMPONENT ARCHITECTURE                 │
└──────────────────────────────────────────────────────────────────┘

CLIENT TIER
═══════════

┌────────────────────────────────────────┐
│      PRESENTATION LAYER                │
│  ┌──────────────┐  ┌──────────────┐   │
│  │Form Builder  │  │Form Renderer │   │
│  │   UI         │  │   UI         │   │
│  └──────────────┘  └──────────────┘   │
│  ┌──────────────┐  ┌──────────────┐   │
│  │Submission    │  │Dashboard     │   │
│  │Management    │  │              │   │
│  └──────────────┘  └──────────────┘   │
└────────────────────────────────────────┘
            │ HTTP/REST
            ▼
APPLICATION TIER
════════════════

┌────────────────────────────────────────┐
│      API LAYER                         │
│  ┌──────────────────────────────────┐ │
│  │  RESTful API Endpoints           │ │
│  │  - Forms, Submissions, Auth      │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
            │
            ▼
┌────────────────────────────────────────┐
│      BUSINESS LOGIC LAYER              │
│  ┌──────────────┐ ┌──────────────┐   │
│  │ Form Service │ │Version       │   │
│  │              │ │Service       │   │
│  └──────────────┘ └──────────────┘   │
│  ┌──────────────┐ ┌──────────────┐   │
│  │Validation    │ │Submission    │   │
│  │Engine        │ │Service       │   │
│  └──────────────┘ └──────────────┘   │
│  ┌──────────────┐ ┌──────────────┐   │
│  │Auth Service  │ │Schema        │   │
│  │              │ │Generator     │   │
│  └──────────────┘ └──────────────┘   │
└────────────────────────────────────────┘
            │ JDBC
            ▼
DATA TIER
═════════

┌────────────────────────────────────────┐
│    REPOSITORY LAYER                    │
│  ┌──────────────────────────────────┐ │
│  │ Data Access Objects (Repositories)
│  │ - FormRepository                │ │
│  │ - VersionRepository             │ │
│  │ - SubmissionRepository          │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
            │ SQL/Transactions
            ▼
┌────────────────────────────────────────┐
│    DATABASE LAYER                      │
│  ┌──────────────────────────────────┐ │
│  │  PostgreSQL Database             │ │
│  │  - Form Metadata Tables          │ │
│  │  - Dynamic Submission Tables     │ │
│  │  - Audit Logs                    │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Form Creation Flow

```
User Input → Validate → Store → Schema → Publish → Ready
   │          │         │       │        │         │
   └─ Code    └─ Rules ─└─ DB  └─ Table └─ Index  └─ Use
     Name
     Desc
```

### Submission Flow

```
Form Data → Client Val → Draft → Server Val → Store → Confirm
   │         │           │       │            │       │
   └─ User   └─ Rules    └─ DB  └─ Rules    └─ Index └─ User
     Input
```

---

*End of UML Diagrams and Flows*
